# IniParser

In-memory ini configuration format parser.

## CMake build

```powershell
cmake -S . -B build
cmake --build build --config Release
```

## Sample ini format

```ini
[Section]
Key=Value
Key2=Value2

[Section2]
Text=Hello World
Number=42
```
